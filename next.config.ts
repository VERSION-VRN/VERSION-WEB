import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Al usar Next.js 16 y Tailwind v4, mantenemos la configuración mínima 
     para dejar que los nuevos motores de resolución funcionen de forma nativa. */
};

export default nextConfig;
